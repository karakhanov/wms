# Логотип Bashkent

Файл **`bashkent-logo.png`** используется в сайдбаре и на экране входа.

Чтобы обновить логотип: замените этот PNG (желательно с прозрачным или тёмным фоном под тёмную тему) или измените путь в `frontend/src/siteConfig.js` (`BRAND_LOGO_URL`).
