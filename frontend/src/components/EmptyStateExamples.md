/**
 * ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ EmptyState с разными вариантами иллюстраций
 * 
 * Файл: src/components/EmptyState.jsx
 * Props:
 *   - title: заголовок (по умолчанию "Нет данных")
 *   - hint: подсказка/поясняющий текст
 *   - variant: 'receipt' | 'success' | 'orders' | 'generic' (default: 'generic')
 *   - actionLabel: текст кнопки действия
 *   - onAction: callback функция кнопки
 *   - actionLink: ссылка для link-варианта
 *   - className: доп CSS класс
 */

// ============================================================================
// 1. ПРИЁМ ТОВАРА (Receipts page) — вариант "receipt"
// ============================================================================
// Файл: src/pages/Receipts.jsx

<EmptyState
  variant="receipt"
  title="Приёмок пока нет"
  hint="Создайте первую приёмку товара для отслеживания поступлений"
  actionLabel="+ Создать приёмку"
  onAction={openCreate}
/>

// ============================================================================
// 2. ОТЧЁТЫ / НЕХВАТКА (Reports, Shortage page) — вариант "success"
// ============================================================================
// Файл: src/pages/Reports.jsx или Dashboard.jsx (shortage widget)

<EmptyState
  variant="success"
  title="Все позиции в норме"
  hint="Минимальные остатки соблюдены. Нехватки не выявлены."
/>

// ============================================================================
// 3. ЗАКАЗЫ (Orders page, dashboard widget) — вариант "orders"
// ============================================================================
// Файл: src/pages/Dashboard.jsx (recent orders widget)

<EmptyState
  variant="orders"
  title="Заказов ещё нет"
  hint="Создайте первый заказ или свяжитесь с менеджером"
  actionLink="/orders"
/>

// Альтернатива с кнопкой:
<EmptyState
  variant="orders"
  title="Заказов ещё нет"
  actionLabel="Перейти к заказам"
  onAction={() => navigate('/orders')}
/>

// ============================================================================
// 4. ДРУГИЕ КОНТЕКСТЫ (по умолчанию generic) — вариант "generic"
// ============================================================================
// Файл: src/pages/Products.jsx (когда нет товаров)

<EmptyState
  variant="generic"
  title="Товаров ещё нет"
  hint="Добавьте первый товар через каталог"
  actionLabel="+ Добавить товар"
  onAction={openCreate}
/>

// Автоматический выбор варианта в зависимости от страницы:
const emptyStateVariant = {
  receipts: 'receipt',
  orders: 'orders',
  reports: 'success',
  shortage: 'success',
  default: 'generic',
}[pageType]

// ============================================================================
// ЦВЕТОВАЯ ПАЛИТРА (соответствует бренду Bashkent WMS)
// ============================================================================
// Основной тейл: #1B6B4A (dark green)
// Акцент: #3B82F6 (blue)
// Используется в SVG иллюстрациях:
//   - fill="#1B6B4A" для основных элементов
//   - fill="#3B82F6" для деталей/акцентов
//   - opacity для создания глубины

// ============================================================================
// АНИМАЦИЯ
// ============================================================================
// Иллюстрации имеют float анимацию (вверх-вниз за 3с)
// CSS: animation: float 3s ease-in-out infinite;

// ============================================================================
// SPACING & LAYOUT
// ============================================================================
// - Минимальная высота: 300px
// - Иллюстрация: 120×120px
// - Gap между элементами: 1rem
// - Padding: 2.5rem by default
// - Текст по центру, макс-ширина для читаемости
